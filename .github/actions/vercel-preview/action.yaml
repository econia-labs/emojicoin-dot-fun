# check-workflow: exempt
description: 'Build a Vercel preview of the project'
name: 'Vercel preview'
runs:
  steps:
  - uses: actions/setup-node@v4
    with:
      node-version: 20
      cache: 'pnpm'
      cache-dependency-path: 'src/typescript/frontend/pnpm-lock.yaml'
  - uses: pnpm/action-setup@v4
    with:
      run_install: false
  using: 'composite'
...
